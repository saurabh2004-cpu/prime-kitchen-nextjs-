/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
    domains: ["picsum.photos", "randomuser.me","img.youtube.com", "i.ytimg.com","images.unsplash.com"],
  },
};

export default nextConfig;
